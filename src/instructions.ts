/** Short usage guidance surfaced by MCP clients during server initialization. */
export const SERVER_INSTRUCTIONS = `Use op_run when a local command needs a 1Password secret without putting plaintext in the MCP transcript. Put op:// references only in op_run's env values; command and argv text are not resolved and may be visible to the OS. argv directly spawns a real executable with no shell or variable expansion. command uses a shell: Windows defaults to cmd.exe (%VAR%); choose shell "powershell" for $env:VAR. Never rely on bare bash/sh on Windows: it may be WSL, where Windows env does not cross unless forwardEnvToWsl is explicitly enabled; use git-bash, wsl, or a native shell deliberately. Redaction protects fully buffered returned stdout, stderr, and error text only—not files, network traffic, process arguments, child processes, transformed encodings, or OS process listings.`;
